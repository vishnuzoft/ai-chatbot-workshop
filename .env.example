# Target backend SSE API (replace with your actual backend URL)
BACKEND_CHAT_API_URL=https://your-agent-backend-url/api/agent/chat

# Client-facing endpoint (uses /api/chat proxy route to eliminate browser CORS errors)
NEXT_PUBLIC_CHAT_API_URL=/api/chat
