# Target backend SSE API (called server-to-server with zero CORS restriction)
BACKEND_CHAT_API_URL=http://192.168.68.111:8070/api/agent/chat

# Client-facing endpoint (uses /api/chat proxy route to eliminate browser CORS errors)
NEXT_PUBLIC_CHAT_API_URL=/api/chat
